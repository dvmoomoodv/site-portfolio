import React from 'react';

const Logo = ({match}) => {
    return (
        <div className="logo">
           <div>
                <span>MH</span>
           </div>
        </div>
    );
};

export default Logo;